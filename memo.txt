connection_diary/
├── index.php              (トップページ)
├── config/
│   └── config.php        (DB設定)
├── database/
│   └── table.php         (テーブル作成)
├── record/
│   └── record.html       (音声録音画面)
└── api/
    └── save_audio_text.php (保存API)

git init
git add .
git commit -m "first commit"
git remote add origin url
git push -u origin master

composer require openai-php/client
composer require vlucas/phpdotenv

cd backend/gpt_api 
# Composerインストーラーをダウンロード
C:\xampp\php\php.exe -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"
# Composerをインストール
C:\xampp\php\php.exe composer-setup.php
# インストーラーを削除
C:\xampp\php\php.exe -r "unlink('composer-setup.php');"

# OpenAI PHPクライアントをインストール
C:\xampp\php\php.exe composer.phar require openai-php/client
C:\xampp\php\php.exe composer.phar require vlucas/phpdotenv

